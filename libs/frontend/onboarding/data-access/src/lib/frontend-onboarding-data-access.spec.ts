import { frontendOnboardingDataAccess } from './frontend-onboarding-data-access';

describe('frontendOnboardingDataAccess', () => {
  it('should work', () => {
    expect(frontendOnboardingDataAccess()).toEqual(
      'frontend-onboarding-data-access'
    );
  });
});
